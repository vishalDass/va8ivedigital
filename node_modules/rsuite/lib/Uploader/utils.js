"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

exports.__esModule = true;
exports.getFiles = exports.guid = void 0;

var _get2 = _interopRequireDefault(require("lodash/get"));

var guid = function guid(num) {
  if (num === void 0) {
    num = 8;
  }

  return (Math.random() * 1e18).toString(36).slice(0, num).toUpperCase();
};

exports.guid = guid;

var getFiles = function getFiles(event) {
  if ((0, _get2.default)(event, 'dataTransfer') && typeof (0, _get2.default)(event, 'dataTransfer') === 'object') {
    return (0, _get2.default)(event, 'dataTransfer.files');
  }

  if (event.target) {
    return (0, _get2.default)(event, 'target.files');
  }

  return [];
};

exports.getFiles = getFiles;