import _get from "lodash/get";
export var guid = function guid(num) {
  if (num === void 0) {
    num = 8;
  }

  return (Math.random() * 1e18).toString(36).slice(0, num).toUpperCase();
};
export var getFiles = function getFiles(event) {
  if (_get(event, 'dataTransfer') && typeof _get(event, 'dataTransfer') === 'object') {
    return _get(event, 'dataTransfer.files');
  }

  if (event.target) {
    return _get(event, 'target.files');
  }

  return [];
};